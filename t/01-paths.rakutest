use Test;
use lib 'lib';
use lib 't/lib';
use App::Racoco::Configuration;
use App::Racoco::Paths;
use App::Racoco::X;
use Fixture;
use TestHelper;
use TestResources;

plan 19;

my $root = TestResources::exam-directory;
sub setup() {
	plan $*plan;
	TestResources::prepare($*subtest) unless $*wo-resources;
}

'01-from-lib'.&test(:6plan, {
	setup();
	indir($root, {
		throws-like { Paths.from(:lib<non-exist>) }, App::Racoco::X::WrongLibPath,
				'throw non-esist';
		throws-like { Paths.from(:lib<non-lib-dir>) }, App::Racoco::X::WrongLibPath,
				'throw non-lib-dir';
		my $p = Paths.from(:lib<exist-lib>);
		ok $p.lib.e, 'lib exist';
		ok $p.root.e, 'root exist';
		ok $p.current-racoco.e, 'racoco exist';
		is $p.lib.parent, $p.root, 'root is parent of lib';
	});
});

'02-root-hash-name'.&test(:1plan, :wo-resources, {
	setup();
	is root-hash-name('/home/user/projects/project'),
		'B8D6346BB8E1B3654A01753655EA8BF0521B17A6',
		'root hash name';
});

'03-current-racoco-in-root'.&test(:1plan, {
	setup();
	my $paths = Fixture::make-paths(TestResources::exam-directory);
	ok $paths.current-racoco.add('.keep').e, 'current racoco in root';
});

'04-current-racoco-out-root'.&test(:1plan, {
	setup();
	my $root = TestResources::exam-directory.add('root');
	my $paths = Paths.new(
		root => $root,
		lib => $root.add('lib'),
		racoco => $root.parent.add('.racoco')
	);
	ok $paths.current-racoco.add('.keep').e, 'current racoco out root';
});

subtest '05-config-path-from-config', {
	plan 1;
	is config-path(root => $*CWD), $*CWD.add(App::Racoco::Paths::CONFIG-FILE),
		'config file from configuration';
}

'06-constructor'.&test(:6plan, {
	setup();
	throws-like { Paths.new(:$root, :lib<non-exist>, :racoco<.racoco>) },
			App::Racoco::X::WrongLibPath, 'throw non-esist';
	throws-like { Paths.new(:$root, :lib<non-lib-dir>, :racoco<.racoco>) },
			App::Racoco::X::WrongLibPath, 'throw non-lib-dir';
	my $p = Paths.new(:$root, :lib<exist-lib>, :racoco<.racoco>);
	ok $p.lib.e, 'lib exist';
	ok $p.root.e, 'root exist';
	ok $p.current-racoco.e, 'racoco exist';
	is $p.lib.parent, $p.root, 'root is parent of lib';
});

'07-coverage-log-path'.&test(:1plan, {
	setup();
	my $paths = Fixture::make-paths(TestResources::exam-directory);
	ok $paths.coverage-log-path.e, 'coverage-log-path';
});

'08-lib-precomp-path'.&test(:1plan, {
	setup();
	my $paths = Fixture::make-paths(TestResources::exam-directory);
	ok $paths.lib-precomp-path.e, 'lib-precomp-path';
});

'09-our-precomp-path'.&test(:1plan, {
	setup();
	my $paths = Fixture::make-paths(TestResources::exam-directory);
	ok $paths.our-precomp-path.add('.keep').e, 'our-precomp-path';
});

'10-index-path'.&test(:1plan, {
	setup();
	my $paths = Fixture::make-paths(TestResources::exam-directory);
	ok $paths.index-path.e, 'index-path';
});

'11-report-data-path'.&test(:1plan, {
	setup();
	my $paths = Fixture::make-paths(TestResources::exam-directory);
	ok $paths.report-data-path.e, 'report-data-path';
});

TestResources::prepare('whole');
my $lib = TestResources::exam-directory.add('lib');

{
	my $racoco-path = racoco-path(:$lib);
	ok $racoco-path.is-absolute, 'racoco path is absolute';
	ok $racoco-path.e, 'racoco path exists';
}

{
	my $precomp-path = our-precomp-path(:$lib);
	ok $precomp-path.is-absolute, 'precomp path is absolute';
	ok $precomp-path.e, 'precomp path exists';
}

{
	nok lib-precomp-path(:$lib).e, 'lib precomp path does not exists by default';
}

{
	is our-precomp-path(:$lib).parent, racoco-path(:$lib), 'out precomp parent';
	isnt our-precomp-path(:$lib), lib-precomp-path(:$lib), 'lib and our precomp';
}

{
  is parent-name($lib), 'exam', 'parent name ok';
}
